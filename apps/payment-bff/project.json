{
  "name": "payment-bff",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/payment-bff/src",
  "tags": ["scope:bff"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/payment-bff"],
      "options": {
        "command": "node build-lambda.js",
        "cwd": "apps/payment-bff",
        "outputPath": "dist/apps/payment-bff",
        "outputFileName": "main.js"
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "payment-bff:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "payment-bff:build:development"
        },
        "production": {
          "buildTarget": "payment-bff:build:production"
        }
      }
    },
    "test": {
      "options": {
        "passWithNoTests": true
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "cd dist/apps/payment-bff && zip -r ./deployment.zip ."
      }
    }
  }
}
