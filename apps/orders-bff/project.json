{
  "name": "orders-bff",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/orders-bff/src",
  "tags": ["scope:bff"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/orders-bff"],
      "options": {
        "command": "node build-lambda.js",
        "cwd": "apps/orders-bff",
        "outputPath": "dist/apps/orders-bff",
        "outputFileName": "main.js"
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "orders-bff:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "orders-bff:build:development"
        },
        "production": {
          "buildTarget": "orders-bff:build:production"
        }
      }
    },
    "test": {
      "options": {
        "passWithNoTests": true
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "cd dist/apps/orders-bff && zip -r ./deployment.zip ."
      }
    }
  }
}
