{
  "name": "engage-bff",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/engage-bff/src",
  "tags": ["scope:bff"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/engage-bff"],
      "options": {
        "command": "node build-lambda.js",
        "cwd": "apps/engage-bff",
        "outputPath": "dist/apps/engage-bff",
        "outputFileName": "main.js"
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "engage-bff:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "engage-bff:build:development"
        },
        "production": {
          "buildTarget": "engage-bff:build:production"
        }
      }
    },
    "test": {
      "options": {
        "passWithNoTests": true
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "cd dist/apps/engage-bff && zip -r ./deployment.zip ."
      }
    }
  }
}
