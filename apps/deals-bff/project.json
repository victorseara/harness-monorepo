{
  "name": "deals-bff",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/deals-bff/src",
  "tags": ["scope:bff"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/deals-bff"],
      "options": {
        "command": "node build-lambda.js",
        "cwd": "apps/deals-bff",
        "outputPath": "dist/apps/deals-bff",
        "outputFileName": "main.js"
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "deals-bff:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "deals-bff:build:development"
        },
        "production": {
          "buildTarget": "deals-bff:build:production"
        }
      }
    },
    "test": {
      "options": {
        "passWithNoTests": true
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "{workspaceRoot}",
        "command": "cd dist/apps/deals-bff && zip -r ./deployment.zip ."
      }
    }
  }
}
